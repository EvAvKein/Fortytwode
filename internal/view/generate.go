package view

// The *_templ.go files in the ./pages and ./profile subpackages are generated from
// the .templ files by templ (pinned as a go.mod tool). templ generate walks this
// directory recursively, so this one directive covers both. Regenerate after
// editing any .templ file with: go generate ./...
//
//go:generate go tool templ generate
