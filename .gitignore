/fortytwode
.env
.token.json
output/
*.log

# database dumps from `make backup`
backups/

# templ output — generated locally via `go generate ./...`, not versioned
*_templ.go

# Cloudflare trusted-proxy ranges, regenerated by `make cloudflare-ips` on every prod deploy
deploy/nginx/cloudflare_realip.conf*
